# Log Summary Conventions

- Training logs: `train_debug.log` (debug) and stdout (info).
- Inference logs: `inference.log` plus stdout.
- Prefer logging errors before raising exceptions in CLI scripts.
