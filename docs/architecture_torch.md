# PyTorch Architecture

This branch does not contain a populated PyTorch implementation. The `ptycho_torch/` directory
only holds bytecode cache files. If a PyTorch port is added later, document the entrypoints and
training workflow here and link to the relevant modules.
