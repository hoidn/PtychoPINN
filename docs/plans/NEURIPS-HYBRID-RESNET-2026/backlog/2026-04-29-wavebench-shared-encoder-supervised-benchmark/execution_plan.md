# WaveBench Shared-Encoder Supervised Benchmark Planning Target

Status: pending fresh workflow plan.

This file exists so active backlog manifest validation has a stable, safe
`plan_path`. It is not implementation authority.

Before implementation, the backlog workflow must draft and approve a fresh plan
for `2026-04-29-wavebench-shared-encoder-supervised-benchmark` using the
completed WaveBench inverse-source preflight summary, the WaveBench provisioning
decision, and native-baseline status. If those gates are not satisfied, the
item should remain gated or be narrowed rather than run. The first supervised
comparison should include both `C=32` and `C=64` unless `C=64` has a recorded
row-level blocker.
