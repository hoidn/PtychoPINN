# WaveBench Provisioning Decision Planning Target

Status: pending fresh workflow plan.

This file exists so active backlog manifest validation has a stable, safe
`plan_path`. It is not implementation authority.

Before implementation, the backlog workflow must draft and approve a fresh plan
for `2026-05-06-wavebench-provisioning-decision` using the completed
WaveBench inverse-source preflight summary and metadata. The plan should clear
or explicitly preserve the dataset staging, environment, and native-baseline
provenance blockers before any WaveBench training item is selected.
