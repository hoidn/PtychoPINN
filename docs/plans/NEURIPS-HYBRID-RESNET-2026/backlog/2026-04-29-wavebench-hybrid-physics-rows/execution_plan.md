# WaveBench Hybrid Physics Rows Planning Target

Status: pending fresh workflow plan.

This file exists so active backlog manifest validation has a stable, safe
`plan_path`. It is not implementation authority.

Before implementation, the backlog workflow must draft and approve a fresh plan
for `2026-04-29-wavebench-hybrid-physics-rows` using completed supervised
WaveBench results and a passed forward-model validation report. If either gate
is missing, the item should remain gated or be narrowed rather than run.
