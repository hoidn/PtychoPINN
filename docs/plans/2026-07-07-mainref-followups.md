# Main-Parity Follow-ups: Gate Closure, Deferred Fixes, Upstream Kit

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to execute this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Ledger: `.superpowers/sdd/progress.md`; briefs/reports under `.superpowers/sdd/ext/`.

**Goal:** Close the two remaining grid_lines-gate factors (N=128, dense-grid geometry) with the proven rect+real_imag+poisson recipe; land the deferred small fixes; reconcile the parent gs2 plan; prepare (not push) the clerical upstream repair kit for origin/main.

**Architecture:** Follow-on to `docs/plans/2026-07-07-gs2-main-parity-reference.md` (complete; 12 review-gated commits, Verdicts A/B recorded, C3a exonerated the object class). All experiments run through `scripts/studies/varpro_probe_ablation_runner.py` with the object-frame metrics landed in `8e892205`. Reference-quality anchors for interpretation: lines_N64 gs2 = gauged amp MAE **0.036** / pearson **0.916**; deadleaves_N64 gs2 = **0.112 / 0.619**; flat-baseline MAE ≈ **0.13**.

**Tech stack:** PyTorch (ptycho_torch), pytest, existing flat count-convention dataset builders, SDD subagents (haiku/sonnet/opus routine, fable for hard debugging).

## User decisions already adjudicated (2026-07-07)

1. Upstream repair kit: IN this plan, executed after gate closure (Tasks 2–3), **clerical-only** (15 CLI/plumbing defects + normalize_data overflow); physics fixes (P1 probe_scale, VP VarPro units) proposed separately later with their own justification. PRs prepared locally; **no push to public `origin` without per-PR approval**.
2. Neurips: the stale `/home/ollie/Documents/neurips` pointer is DROPPED from project CLAUDE.md (done in the commit that lands this plan). No paper-citation check.
3. Push policy: `fno-stable` is pushed to the **`internal`** remote (`hoidn/PtychoPINN-internal`) now and after each subsequent commit batch. Public `origin` untouched.
4. Gate thresholds: proposed by Task 4's memo and adjudicated by the user WITH data in hand (not pre-committed here).

## Global Constraints

- Frozen files (no edits): `ptycho/model.py`, `ptycho/diffsim.py`, `ptycho/tf_helper.py`, `ptycho/config/config.py`, `ptycho_torch/config_params.py`, `ptycho_torch/model.py`. The 2026-07-07 model.py authorization was one-defect-only and is CLOSED.
- Frozen datasets (read-only, never regenerate): `.artifacts/integration/grid_lines_gs2/datasets/`, `.artifacts/varpro_ablation/datasets/` existing files. New derived datasets get NEW filenames + provenance JSON in `.artifacts/varpro_ablation/datasets/`.
- Commits: surgical staging (never `git add -A`; other sessions share the branch); the word "claude" must never appear in commit messages; no `--no-verify`; artifacts never committed.
- GPU: training never on CPU; ≥6144 MiB headroom gate before each launch (≥10240 for N=128 arms); PID-tracked (`cmd & pid=$!; wait "$pid"`); batch 8 at N=128, batch 16 at N=64.
- Interpretation metrics: object-frame basis (`amp_mae_objframe_gauged`, `amp_pearson_objframe`, `patch_amp_pearson_mean`, `patch_lsq_scalar_median`) + visual check of `reconstruction_grid.png`. Raw `amp_mae` reported for continuity with the old gate.
- Python via PATH `python` (PYTHON-ENV-001).

---

### Task 1: Runner N override + shape fail-fast (SDD implementer, TDD)

**Files:** Modify: `scripts/studies/varpro_probe_ablation_runner.py`; Test: `tests/torch/test_varpro_probe_ablation_runner.py`.

**Interfaces:** Produces: CLI flag `--N <int>` (optional; default None = arm-table value) that overrides the arm config's `"N"` before config/dataset construction; a fail-fast `ValueError` when the effective N mismatches the train-npz `diff3d` frame size (message names both values and the `--N` remedy).

- [x] **Step 1 (failing tests):** (i) `--N 128` plumbs into the built config (assert effective DataConfig/arm dict N == 128 for a base arm); (ii) mismatch fail-fast: N=64 arm + a synthetic 128×128-frame npz in tmp_path raises `ValueError` naming 64, 128, and `--N` (this pins the crash from the 2026-07-07 first N=128 attempt — `RuntimeError` deep in mmap probe allocation — into a one-line diagnosis); (iii) default path unchanged (existing exact-dict arm pins stay green untouched).
- [x] **Step 2:** implement; keep the override a single transformation applied where the arm dict is materialized (do not fork per-arm logic).
- [x] **Step 3:** full runner test file green (`python -m pytest tests/torch/test_varpro_probe_ablation_runner.py -q`); ONE commit; review gate. *(Done 2026-07-08: commit `6ad8a880`, 67 passed = 63 pre-existing + 4 new, review PASS zero findings.)*

### Task 2: lines_N128 discriminator — isolate the N factor (controller, GPU)

lines_N128 is K-NN sampled (not dense-grid), so with Task 1 it isolates **N** alone. Datasets exist: `.artifacts/varpro_ablation/datasets/lines_N128_{train,test}.npz` (counts convention, nphotons 1e9).

- [x] **Step 1:** remove the stale `.artifacts/varpro_ablation/c3_lines128/` (contains only the 2026-07-07 crash logs — verify no summary.json before deleting). *(Done 2026-07-08: crash signature confirmed in both logs, no summary.json, removed.)*
- [x] **Step 2:** run `gs2_probe_trainable` then `gs1_trainable`, 25 ep, batch 8, `--N 128`, headroom gate ≥10240 MiB, PID-tracked, output `.artifacts/varpro_ablation/c3_lines128/` (adapt scratchpad launcher `run_c3_lines64.sh`). *(Done 2026-07-08: both arms rc=0, --N 128 verified in arm_config, no crash.)*
- [x] **Step 3 (readout + record):** objframe metrics + visual vs the lines_N64 anchor (0.036/0.916). **Interpretation rule:** comparable (gauged MAE ≲ 0.1, pearson ≳ 0.7, structured visual) ⇒ N innocent → Task 3 proceeds; materially degraded ⇒ **N is the gate boundary** → record in ledger + findings candidate, SKIP Task 3, jump to Task 4 (gate must either move to N=64 or the recipe adapted for N=128 — memo material).
  **RESULT (2026-07-08): MATERIALLY DEGRADED — flat-amplitude collapse at BOTH gridsizes.** gs2: objframe pearson −0.016, patch pearson 0.004, canvas_amp_std 0.0000; gs1: 0.046 / 0.026 / 0.001. Gauged MAE 0.073 is a flat-image artifact (pearson ≈ 0); raw amp_mae 0.46 deceptively matches the N=64 pass. Visual: uniform white footprint, zero line structure. Failure is genuine, not registration (per-patch fidelity ≈ 0 PRE-reassembly); converged, not undertrained (val loss plateaued: gs2 3.3e3→3.01e3, gs1 906). Cross-check: mainref A2 verdicts were measured on deadleaves_N64 — NO recipe/lineage has ever reconstructed N=128 in this investigation. **N=128 alone (K-NN sampling, no dense geometry) breaks the count recipe ⇒ Task 3 SKIPPED per rule; findings row lands with Task 4 Step 3.**

### Task 3 (conditional on Task 2 pass): dense-grid geometry test — **SKIPPED 2026-07-08 (Task 2 failed; N=128 alone breaks the recipe, so the dense-grid factor is untestable until the recipe reconstructs at N=128)**

> **RESURRECTED (2026-07-08 user adjudication, "geometry first"):** with the Task 9 RCA delivering a passing N=128 vehicle (hybrid_resnet + count recipe, T2 pass at both gridsizes), the gate closes ONLY after the dense-4px-grid geometry factor is exercised. Execute Steps 1–3 below with `--architecture hybrid_resnet`; interpretation anchors: K-NN hybres gs2 = 0.934/0.795/0.026/0.107. Pass ⇒ gate closes with full original-intent coverage; fail ⇒ dense-overlap geometry isolated as its own finding.
>
> **RESULT (2026-07-08): PASS — GATE CLOSED with full original-intent coverage.** hybres + count recipe + N=128 + gs2 on the gate-extracted dense-4px-grid geometry (`gridgeom_N128`, builder commit `3d28ffae`, provenance-verified dose 1.77e6 photons/image): objframe pearson **0.985** / patch pearson **0.860** / gauged MAE **0.0158** / canvas_amp_std **0.139** — every metric BETTER than the K-NN anchor (0.934/0.795/0.026/0.107); dense overlap helps, not hurts. 25/25 epochs (best val 624.85, still improving). Artifacts: `.artifacts/varpro_ablation/task3_gridgeom/hybres_gridgeom_n128_gs2/`. Operational note: training survived but the metrics phase OOM'd the machine 3× before `69c54efc` (collect_objframe_patches single-batch bomb, see ledger cont52–54); the recorded numbers come from the fixed, 48G-capped replay.
>
> **SEED-ROBUSTNESS CONTROL (2026-07-08, user challenge "maybe cnn@128 just fails on that seed"):** all prior runs hardcoded seed 42 (`train_lightning_only.py` `set_seed`; plumbing added in `269f7d68`). Controls at seeds 11 and 17 (identical protocol: gs1_trainable/cnn, count recipe, lines_N128, 25 ep, batch 8, 34 batches/epoch matching the original): BOTH collapse — seed 11 objframe pearson 0.082 / patch 0.044 / canvas_std 0.0010; seed 17 = 0.071 / 0.036 / 0.0015; distinct trajectories confirmed (trained s1/s2 0.49/1.13 vs 1.01/0.82 vs seed-42's ≈1.0/1.0). 4/4 collapsed draws across 3 seeds (incl. gs2\@42) ⇒ the collapse is seed-robust, not a convergence lottery; TORCH-N128-FLAT-AMP-001 amended. Artifacts: `.artifacts/varpro_ablation/task9_seed/`.
>
> **Dose facts (measured 2026-07-08, user-mandated documentation; refined after user challenge):** the provenance `nphotons=1e9` field is INERT legacy metadata — the binding dose parameter is `target_mean_count: 108.0` counts/pixel (fly001-matched; provenance records counts_mean 107.99; zero uint16 saturation, max pixel 1628). Photons/image = target_mean_count × N² exactly: lines_N128 1.77e6 (meets the ≥1e6 synthetic-Poisson bar), lines_N64 4.42e5 (BELOW the bar — any N=64 dataset at 108 counts/px is structurally below it; would need target_mean_count ≥ ~245). "nphotons 1e9" labels in prior plan/ledger text are misleading and superseded by this note. The gridgeom dataset must report its MEASURED photons/image in its provenance JSON and meet ≥1e6 (at N=128 with target_mean_count=108 it will).

**Decision (controller):** regenerate a NEW flat count-convention dataset with the gate's geometry rather than converting the frozen grouped file — the flat builder lineage exists (it produced `lines_N64/128` and `deadleaves_*`; provenance `provenance_lines.json` names the generating task) and regeneration keeps provenance clean. Grouped→flat conversion of the frozen npz is the fallback if the builder can't reproduce the gate's scan pattern.

**Files:** Create: `scripts/studies/make_gridgeom_flat_dataset.py` (or extend the existing builder script — implementer locates it via `provenance_lines.json` / ledger and reuses, not duplicates); Output: `.artifacts/varpro_ablation/datasets/gridgeom_N128_{train,test}.npz` + `provenance_gridgeom.json`.

- [ ] **Step 1 (implementer, small):** builder producing flat RawData-format npz (`xcoords/ycoords/diff3d(uint16 counts)/probeGuess/objectGuess/scan_index/ground_truth_patches`) with: lines-family object, **dense 4 px grid scan** matching the gate dataset's geometry (extract span/pitch from the frozen `.artifacts/integration/grid_lines_gs2/datasets/N128/gs2/train.npz` coords — read-only), N=128, nphotons 1e9, counts convention `round(amp²·S)`. Unit test: shape/dtype/key contract + coords pitch == 4 px. Provenance JSON per the existing pattern. Commit + review gate.
- [ ] **Step 2 (controller, GPU):** same two arms as Task 2 on the gridgeom data, output `.artifacts/varpro_ablation/c3_gridgeom128/`.
- [ ] **Step 3 (readout + record):** pass ⇒ the full gate recipe works on gate-geometry data — the 2026-07-06 gate is closable on the rect recipe (Task 4 memo proposes the numbers); fail ⇒ **dense overlap geometry is the isolated cause** (~97% intra-group overlap) — findings row + gate recalibration proposal in the Task 4 memo.

### Task 4: Gate adjudication memo + parent-plan reconciliation (analysis subagent → user)

- [ ] **Step 1 (read-only subagent, opus):** memo covering (a) proposed gate definition for plan-2026-07-06 Task 3 on the rect recipe + objframe basis, with threshold options derived from the Task 2/3 numbers (report BOTH a raw-amp_mae continuity value and objframe gauged MAE + patch pearson); (b) per-item disposition proposal (retire / rescope / keep, with rationale) for parent-plan open work: board #25 (hybres@gs2 training), #26 (group_merge option), #27 (integration gate + docs), and in-progress #23/#34/#42/#43/#44 (gs2 Tasks 3/3b–3e) — each was scoped when the dict-path amplitude/MAE recipe was the gate vehicle. Memo to `.superpowers/sdd/ext/task-gate-adjudication-memo.md`.
- [x] **Step 2 (user adjudication):** present the memo; user decides gate definition + task dispositions.
  **ADJUDICATED (2026-07-08):** (1) Gate vehicle = **modified G-B**: gate closure BLOCKS on the N=128 collapse RCA; the RCA must use the known-working hybrid_resnet N=128 paths (study index, hybrid_resnet integration test, e.g.) as positive reference — N=128 reconstruction is proven achievable in this codebase, so the collapse is recipe/architecture-conditional, not a universal N wall. (2) Thresholds = **T2** (objframe pearson ≥ 0.60, patch pearson ≥ 0.40, gauged MAE ≤ 0.12, canvas_amp_std ≥ 0.03; raw amp_mae = annotated continuity row only). (3) Dispositions accepted **except** the N=64-closure consequence: #34/#42/#43/#44 RETIRE (fixes kept, moot MAE-gate re-runs cancelled) and #26 KEEP stand; #23/#25/#27 adopt the T2/objframe basis but BLOCK on the Task 9 RCA rather than closing at N=64. (4) Task 8 = **R2 defer** (Step 1 re-baseline only; Step 3 held behind the RCA).
- [x] **Step 3 (docs commit):** update `docs/plans/2026-07-06-generator-gridsize2-support.md` + the task board per the decisions; findings row(s) for Task 2/3 outcomes; ledger. Review gate. *(Done 2026-07-08: commit `e73965a4` — parent-plan adjudication note + findings rows `TORCH-N128-FLAT-AMP-001`, `TORCH-REASSEMBLY-COM-STALE-001` (alpha/beta verified orphaned); review PASS zero findings.)*

### Task 9 (added 2026-07-08 by Task 4 adjudication): N=128 recipe-collapse RCA — gate closure blocks on this

**Question:** why does the rect+real_imag+poisson count recipe flat-collapse at N=128 (both gridsizes, converged, K-NN sampling) when (a) the same recipe passes at N=64 (lines 0.916/deadleaves 0.619 pearson) and (b) **hybrid_resnet reconstructs N=128 in this codebase** (user-cited working reference: study index + hybrid_resnet integration test paths)?

- Scope (memo §2.5 G-B minimum + user's reference directive): (i) instrument the forward's effective scale constants (output_scale, probe_scale, Parseval/photon-per-pixel terms — RECT-MAE-UNITS-001 / POISSON-SCALE-001 lineage) at N=64 vs N=128 on identical objects — leading hypothesis is a scale/units N-dependence (evidence: converged + gridsize-independent collapse); (ii) capacity/epoch controls (extended-epoch N=128 run) to rule out optimization; (iii) diff the working hybrid_resnet N=128 configuration against the collapsing CNN count-recipe configuration factor-by-factor (architecture, normalization, scale constants, loss mode) to isolate the interaction.
- Blocks: parent-plan gate closure, #23/#25/#27, Task 8 Step 3. Model policy: sonnet instrumentation, fable for RCA synthesis if the factor isolation resists.
- **RESOLVED (2026-07-08):** root cause = CNN-architecture × N=128 × sparse-count-Poisson optimization collapse (constant output, cross-patch variation 5.7e-8); scale/units hypothesis REFUTED with numbers (output_scale 24.03 vs 24.32; s1/s2 ≈ 1.0 both N); 50-epoch control does not escape (pearson 0.071); **hybrid_resnet on the identical count recipe at N=128 gs1 reconstructs and passes T2 with margin** (0.872 / 0.731 / gauged MAE 0.035 / canvas_std 0.105 — `.artifacts/varpro_ablation/task9_rca/hybres_count_n128/`). Findings row TORCH-N128-FLAT-AMP-001 updated. Open scoped follow-up: CNN-internal mechanism (silu-escape floor vs bottleneck capacity). Report: `.superpowers/sdd/ext/followups-task9-rca-report.md`. Consequence: the gate is passable at N=128 with the hybrid_resnet vehicle — gs2 confirmation run (= rescoped #25) launched next; gate-closure semantics go to the user with that result.
- **gs2 CONFIRMED (2026-07-08):** hybrid_resnet + count recipe + N=128 at gridsize 2 passes T2 with wide margin — objframe pearson **0.934**, patch pearson **0.795**, gauged MAE **0.0255**, canvas_std **0.107** (better than the gs1 control 0.872 and the N=64 lines anchor 0.916); visual structured (fine lines soft at 25 ep). Artifacts: `.artifacts/varpro_ablation/task9_rca/hybres_count_n128_gs2/`. T2 evidence now complete at BOTH gridsizes at N=128. Remaining unexercised gate factor: dense-4px-grid geometry (skipped Task 3) — user decides whether closure requires it.
- **Task 9b MECHANISM DECOMPOSED (2026-07-08):** the CNN-internal mechanism follow-up is closed. CPU forward-hook instrumentation of the trained checkpoints localizes the collapse to the encoder: the N-conditional depth branch (`ptycho_torch/model.py` `Encoder.__init__`, 3 downsampling stages at N=64 vs 4 at N=128) × trained-weight ReLU dead-fraction compounding (68→79→91→97% across the 4 stages; N=64 flat ~0.60–0.65) drives bottleneck cross-patch CV to 0.0037 (vs 0.381 at N=64) before the decoder sees anything informative. Refuted with measurements: silu-escape floor (collapsed output 0.528/0.681 vs floors −1.08/−1.48), input-sparsity trigger (matched-scale random noise reproduces the identical cascade), weight degeneracy (26/26 conv trained/fresh norm ratios 0.95–1.37). hybrid_resnet avoids both triggers structurally (fixed 2-stage downsample independent of N; residual+GELU blocks). No GPU control needed; zero repo edits. Findings row TORCH-N128-FLAT-AMP-001 amended; dose facts landed as SYNTH-POISSON-DOSE-001 (target_mean_count binding, nphotons inert, ≥1e6 measured photons/image requirement, N=64@108 counts/px below bar, dose NOT the collapse driver). Open (cheap, unscheduled): CBAM-gating contribution via frozen-checkpoint monkeypatch. Report: `.superpowers/sdd/ext/followups-task9b-report.md`.

### Task 5: Deferred-fixes batch (SDD implementer, TDD, ONE commit)

All three carry exact prescriptions from the 2026-07-07 reviews (ledger cont30; final-review Minor #3/#5; B4a review COULD-1):

**Files:** Modify: `ptycho_torch/reassembly.py`, `scripts/studies/varpro_probe_ablation_runner.py`; Tests: `tests/torch/test_varpro_solve_units.py`, `tests/torch/test_varpro_probe_ablation_runner.py`.

- [ ] **Step 1:** fix the inverted `'com'` conditional (`reassembly.py` ~:219-223 and ~:1207-1211: `if 'com' in data_dict:` derives COM from coords, `else:` reads `data_dict['com']` — branches swapped; the else would KeyError). Update `_SyntheticVarProDataset` in `test_varpro_solve_units.py`, which currently sets `{"com": None}` relying on the inversion. Failing test first: a dataset WITH a real `'com'` value must use it; one WITHOUT must derive from coords.
- [ ] **Step 2:** `compute_objframe_metrics` zero-coverage fail-fast: `if n_used == 0: raise ValueError(f"compute_objframe_metrics: 0/{n_total} patches placed in-bounds — check coords_global units/frame")` before gauge/pearson (currently silent NaN, non-strict JSON). Failing test: all-out-of-bounds coords raise.
- [ ] **Step 3:** guard the verbose `Scalars solved ... (effective output_scale = ...)` print (`reassembly.py` ~:1420): print a safe representation when `output_scale` is None (amplitude mode) and avoid NameError on an empty loader.
- [x] **Step 4:** selectors green (`test_varpro_solve_units.py` + both runner/reassembly files); ONE commit; review gate. Byte-identity caution: none of these may change any computed metric on the covered paths — the 'com' fix must be shown numerically inert for datasets that always carry `'com'` (the production case).
  *(Done 2026-07-08: commit `229a0c5b`, 88 passed, review PASS zero findings with independent RED/GREEN reproduction. AMENDED mid-task on implementer evidence: the stored `data_dict['com']` is a stale last-file centroid (`dataloader.py::memory_map_data` unconditional per-file overwrite) — the prescribed branch swap would have mis-centered multi-file reconstructions, so the conditional was REMOVED entirely (always derive from coords; inert by construction; kills the comless KeyError). Follow-up material recorded in the report: dataloader dead 'com' store; identical inverted conditional persists in out-of-scope `reassembly_alpha.py`/`reassembly_beta.py`.)*

### Task 6: Upstream repair kit for origin/main — **RESCOPED 2026-07-08 (user): folded into the rebase plan** (`docs/plans/2026-07-07-rebase-fno-stable-onto-main.md`, whose corrected overlay + replay carries all fno-stable fixes into main). Task 6 reduces to: during rebase-plan execution, verify the 15-defect catalog + normalize_data fix are covered by the replayed tree (checklist against MAINREF-CLI-001). No standalone main-cli-repairs branch.

Source material: `.artifacts/integration/grid_lines_gs2/mainref_evidence/reference_provenance.md` + `reference_provenance_patch.diff` (15 defects, clone-verified) + `NORMALIZE-DATA-UINT16-001`.

- [ ] **Step 1 (controller):** `git fetch origin && git switch -c main-cli-repairs origin/main` — LOCAL branch only. (No worktrees/clones; suspend fno-stable work during Task 6's implementer sessions or serialize.)
- [ ] **Step 2 (SDD implementers, one commit per defect family):** (1) dataclass fields (defects 1, 12, 13); (2) config_bridge getattr defaults (2–5); (3) helper + registry ports (6, 8 — main's own tests reference these; suite likely red at HEAD, verify first and cite in the PR); (4) n_images clamp (7); (5) npz batch-contract adapters (9, 10); (6) counts-support adapter (11); (7) inference stitcher fix (14) + input-scale fix (15); (8) normalize_data overflow (port of `dc1f3e74`). Each: tests (reuse the clone-validated approach + main's own suite), review gate, PR description citing MAINREF-CLI-001 and the evidence bundle.
- [ ] **Step 3 (user, per-PR):** present the series; push/PR to public origin ONLY on explicit per-PR approval.

### Task 7: Housekeeping (rolling)

- [x] Drop the stale neurips pointer from project CLAUDE.md (lands with this plan's commit).
- [x] Push `fno-stable` to `internal` now, and after each Task's commit batch (`git push internal fno-stable`). Never push `origin` without approval. *(Rolling pushes maintained throughout; final batch pushed 2026-07-08.)*
- [x] Ledger entries per task; this plan's checkboxes updated as tasks close; light final review over this plan's code commits (Tasks 1, 3, 5) at the end. *(Final review 2026-07-08, opus, over all 8 code commits (6ad8a880, 229a0c5b, 3d28ffae, 79787e7e, 269f7d68, 69c54efc, 18ff821e, 080b1185): READY TO CLOSE — 95/95 tests, constraint sweep clean, zero must-fix; Minor triage recorded in ledger cont62. Non-blocking follow-ups carried: (1) aligned driver does not propagate per-arm failure to its exit code and the ARMS filter silently skips all arms on a typo — completion contract remains the per-arm `.exit` files; (2) montage truth-crop reads the module BORDER_TRIM_PX default rather than the per-run `border_trim_px` in metrics.json — divergence only under a non-default `--border-trim-px`.)*

### Task 8: 20-epoch aligned-ablation rerun — folded in, amended, adjudication-gated

`docs/plans/2026-07-06-aligned-ablation-20epoch-rerun.md` (H2–H6 on the aligned gs1 N=128 MAE-recipe grid, ~4 h GPU) is folded into this plan but **must not run as written**: two of today's commits changed its evaluation pipeline after its 5-epoch baseline artifacts were recorded — `d5f40106` (barycentric canvas padding moves its 20 px border-crop; no-silent-drop changes patch inclusion) and `22d77509` (probe-weighted reassembly for 2/3-D probes, used by its probe-weighting inference variants). Its VarPro caveat REMAINS in force: the 2026-07-07 VarPro units fix applies only to `rectangular_scaled` mode; these amplitude-mode arms keep the historical basis by design (497d1f69 guard), so `*_varpro` rows stay scale-unreliable.

- [x] **Step 1 (pre-flight re-baseline, controller GPU — cheap, inference-only):** re-evaluate the existing 5-epoch checkpoints (`.artifacts/varpro_ablation/ext_matrix_aligned/`) through the CURRENT pipeline into a new root (`ext_matrix_aligned_rebase/`); record deltas vs the committed 5-epoch numbers. This is the comparability anchor for any 20-epoch conclusion. *(Done 2026-07-08, corrected same day: all 6 arms × 4 variants replayed; `229a0c5b` verified inert; `*_novarpro` deltas ≤3% (d5f40106: canvas 232→264 zero-margin, true dead ring 20→16 px, 20 px crop kept for comparability); `canvas_amp_std` deltas 1–8%; `*_varpro` amp_mae −13–27% + s1/s2 shifts (d755b2ae unconditional crop-band fix; 497d1f69 fold rect-only); twin-arm bit-identity HOLDS both grids. Two earlier claims (3–5× canvas_amp_std drop, bit-identity broken) were a `norm_Y_I` scoring bug in the first glue script — retracted; the corrected rerun cross-checked against the original grid-production script. Report: `.superpowers/sdd/ext/followups-task8-step1-report.md`. BONUS RCA datapoint: cnn arm reconstructs N=128 under MAE/amplitude recipe (5ep corr 0.952) — collapse is recipe-conditional.)*
- [x] **Step 2 (amend the 20ep plan):** dated amendment updating its Invariants — evaluation pipeline = post-`d5f40106`/`22d77509` state, comparisons against the re-baselined (not original) 5-epoch numbers, varpro caveat retained with the amplitude-mode rationale. *(Done 2026-07-08: Amendment section added to `docs/plans/2026-07-06-aligned-ablation-20epoch-rerun.md` — anchor replacement, H2 bit-exact→≤1e-6, varpro caveat restated, execution HELD per R2.)*
- [x] **Step 3 (adjudication-gated execution):** ADJUDICATED 2026-07-08 = **R2 defer**: held behind Task 9 (N=128 RCA); escalate to DROP if the RCA shows the collapse reaches these arms. **RE-ADJUDICATED 2026-07-08 post-RCA = RUN NOW AS AMENDED** (the collapse is CNN×count-Poisson×N=128; these arms are MAE/amplitude recipe, cnn@N128 MAE reconstructs at 0.952 — the drop condition did not trigger). Execute the 20ep plan's Tasks 1–6 as amended, sequenced after the Task 3 gridgeom gate run (GPU serialization). *(Done 2026-07-08: all 5 arms 20 ep exit 0; scoring via repo-ported harness `18ff821e` (1e-6 reproduction gate vs the re-baseline anchor passed first-attempt) + montage `080b1185`; verdicts — H2 bit-exact HOLDS, H3 probe>uniform holds and strengthens (5ep ampphase reversal was a convergence artifact), H4 mostly convergence (gap 0.055→0.015), H5 cnn gap persists (native 2.2×), H6 l2match_on recalibrates amplitude (5.4× native amp MAE, calibration observation). NO 5-epoch conclusion flips; HYBRES-ALIGN-001 stands. Full table: `docs/plans/2026-07-01-varpro-ablation-phase1-findings.md` §"20-epoch rerun".)*

## Execution notes

- Order: Task 1 → 2 → (3) → 4 → 6, with Task 5 slotted anytime (independent); Task 8 Steps 1–2 anytime after Task 5 lands (its re-baseline should use the post-Task-5 pipeline so the 'com'/objframe fixes are in), Step 3 after Task 4; Task 7 rolling. One implementer in the tree at a time.
- Model policy: sonnet for Tasks 1/3/5/8 implementers and small reviews; opus for the Task 4 memo and Task 6 physics-adjacent reviews; fable only if a Task 2/3 result demands hard RCA.
- Self-review (coverage): recommended steps → tasks: N=128 + geometry (1–3), parent-plan reconciliation + gate (4), deferred fixes (5), upstream kit (6), housekeeping/push/CLAUDE.md (7), 20-epoch ablation rerun fold-in (8). All covered; user decisions recorded above; Task 4 memo carries BOTH adjudications (gate/parent-plan items AND the Task 8 rerun disposition).
