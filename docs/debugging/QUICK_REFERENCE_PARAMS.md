# Quick Reference: Params

Key configuration fields in `ptycho/params.py`:

- `N`: diffraction pattern size
- `gridsize`: grouping size
- `offset`: scan spacing for grid data
- `batch_size`, `nepochs`
- `nphotons`: expected photon count
- `model_type`: `pinn` or `supervised`
