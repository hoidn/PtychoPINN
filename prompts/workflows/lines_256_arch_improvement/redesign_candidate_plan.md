Use the injected redesign brief and redesign design note as the authoritative inputs.

Task:
- Write a concrete implementation plan for this one redesign candidate.

Required output:
- Write the implementation plan to the `plan_doc_path` named in the redesign brief.

The plan must include:
- specific files expected to change
- the candidate package files that must be produced
- the minimum targeted checks that should run before review
- any repo or workflow constraints that the implementation must preserve

Constraints:
- Keep the plan specific to this redesign candidate only.
- Do not implement the candidate in this step.
- Do not write `proposal_result.json` in this step.
