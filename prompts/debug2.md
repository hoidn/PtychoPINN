read index.md. then reason about the then reason about the root cause of the bug, as described here: {BUG_DESCRIPTION}
then:
- review the spec files
  - review the implementation plan templates and fiix plan template under plans/
  - draft a debugging plan. you can either extend an existing initiative or create a new one. also update fix_plan as needed
  - write the plan and fix plan update to file, then stage and commit

