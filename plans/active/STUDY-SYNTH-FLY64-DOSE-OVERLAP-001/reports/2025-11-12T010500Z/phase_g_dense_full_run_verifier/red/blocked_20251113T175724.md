# Blocker: Metrics Report Generation Failed

**Timestamp:** $(date -u +"%Y-%m-%dT%H:%M:%SZ")
**Phase:** Phase G metrics aggregation
**Status:** Blocked

## Error Signature

```
ERROR: Required models missing for delta computation: Baseline, PtyChi
Available models: Pty-chi (pty-chi), PtychoPINN
```

## Context

The `report_phase_g_dense_metrics.py` script failed during aggregation because it's looking for models named "Baseline" and "PtyChi" but the actual model names in the metrics_summary.json are:
- "Pty-chi" (with dash and lowercase 'chi')
- "PtychoPINN"

## Impact

This blocks:
1. Creation of `analysis/aggregate_report.md`
2. Creation of `analysis/aggregate_highlights.txt` 
3. Creation of `analysis/metrics_delta_highlights_preview.txt`
4. Post-verify-only execution (depends on preview file)
5. Final verification bundle (10/10 validity check)

## Attempted Command

```bash
python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/report_phase_g_dense_metrics.py \
  --metrics plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier/analysis/metrics_summary.json \
  --output plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier/analysis/aggregate_report.md \
  --highlights plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier/analysis/aggregate_highlights.txt
```

## Next Steps

Need to either:
1. Fix the model name mapping in report_phase_g_dense.py to accept "Pty-chi" as an alias for "PtyChi"
2. Fix the metrics_summary.json generation to use the expected "Baseline"/"PtyChi" names
3. Update the script to be more flexible with model name matching

## Artifacts

- CLI log: `cli/aggregate_report_cli.log`
- Metrics input: `analysis/metrics_summary.json`
