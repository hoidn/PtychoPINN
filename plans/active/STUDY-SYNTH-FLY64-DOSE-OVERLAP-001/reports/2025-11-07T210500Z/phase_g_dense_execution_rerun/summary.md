### Turn Summary
Framed a clean-hub rerun plan for Phase G dense evidence, centering on a new `prepare_hub` helper with explicit `--clobber`.
Mapped RED→GREEN pytest additions and the dense pipeline CLI run so the next loop can capture fresh metrics and manifests.
Next: Ralph implements `prepare_hub`, runs the targeted selectors, executes the rerun with `--clobber`, and records metrics in docs/ledger.
Artifacts: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-07T210500Z/phase_g_dense_execution_rerun/ (plan/plan.md)
