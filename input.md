Brief:
Guard `/home/ollie/Documents/PtychoPINN`, export `AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md` and `HUB=$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier`, then rerun `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv | tee "$HUB"/green/pytest_compare_models_translation_fix_v3.log` so the guarded selector stays GREEN before touching the hub.
Execute `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber |& tee "$HUB"/cli/run_phase_g_dense_stdout.log` and immediately verify the regenerated Phase C/D/E artifacts plus CLI logs live under this hub.
Follow with `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --post-verify-only |& tee "$HUB"/cli/run_phase_g_dense_post_verify_only.log`, then rerun `report_phase_g_dense_metrics.py` and `plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/analyze_dense_metrics.py --hub "$HUB"` so SSIM grid summary/log, verification_report(.json/.log), highlights preview, metrics_summary(.json/.md), metrics_digest.md, and `analysis/artifact_inventory.txt` exist.
Stop only when `analysis/verification_report.json` shows 10/10; otherwise capture `$HUB/red/blocked_$(date -u +%FT%H%M%SZ).md` with the failing command + minimal error signature, update `analysis/blocker.log` and `$HUB/summary/summary.md`, and leave the selector + command details in the artifacts list.

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
