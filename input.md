Brief:
Export `AUTHORITATIVE_CMDS_DOC`/`HUB`, rerun `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv` and capture `green/pytest_compare_models_translation_fix_v18.log` so we know the chunk helper still passes the guard.
Then rerun the chunked compare_models debug slice for both train and test with `--baseline-debug-limit 320 --baseline-chunk-size 160 --baseline-predict-batch-size 16`, teeing to `cli/compare_models_dense_{split}_debug_v3.log`, and confirm the new logs finish past chunk 33 with non-zero DIAGNOSTIC stats (the prior attempt stalls at `analysis/dose_1000/dense/test_debug_v2/logs/logs/debug.log:1299`).
Follow immediately with the full chunked train/test commands (`--baseline-chunk-size 256 --baseline-predict-batch-size 16`, logs `cli/compare_models_dense_{split}_full_v3.log`), and stop for a `$HUB/red/blocked_<timestamp>.md` if `analysis/dose_1000/dense/test/comparison_metrics.csv` or `analysis/metrics_summary.json` still lacks Baseline rows or “Pty-chi (pty-chi)” persists.
Once Baseline metrics exist, run the Phase D selectors (`pytest tests/study/test_dose_overlap_overlap.py::test_filter_dataset_by_mask_handles_scalar_metadata -vv` and `pytest tests/study/test_dose_overlap_overlap.py::test_generate_overlap_views_dense_acceptance_floor -vv`), execute `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber`, regenerate the metrics bundle via `report_phase_g_dense_metrics.py` + `analyze_dense_metrics.py`, and verify the CSV/JSON outputs before moving on.
Finish with `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --post-verify-only`; if the helper still fails (e.g., missing `analysis/metrics_delta_highlights_preview.txt`) or any SSIM/verification/preview artifact is absent, capture the failing command + tail to `$HUB/red/blocked_<timestamp>.md` instead of proceeding.

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
