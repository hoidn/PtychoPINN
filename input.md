Brief:
Work from /home/ollie/Documents/PtychoPINN, export AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md and HUB=$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier, and keep the translation regression guard green via `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv | tee "$HUB"/green/pytest_compare_models_translation_fix_v6.log`.
Instrument/fix scripts/compare_models.py so both the train and test reruns emit non-zero Baseline reconstructions (inspect `$HUB/analysis/dose_1000/dense/{split}/reconstructions_aligned.npz` and stop with `$HUB/red/blocked_<timestamp>.md` if any baseline_complex tensor is still all zeros) before continuing.
Once Baseline metrics look sane, rerun the Phase D acceptance selectors, execute `run_phase_g_dense.py --clobber --dose 1000 --view dense --splits train test`, and immediately replay `report_phase_g_dense_metrics.py` plus `bin/analyze_dense_metrics.py` so metrics_summary.json, the digest, preview text, and artifact inventory are refreshed.
Finish by running the fully parameterized `run_phase_g_dense.py --post-verify-only`, make sure `{analysis}` now contains the SSIM grid, verification logs/report, highlights checker logs, preview, and artifact inventory with `verification_report.json` reporting 10/10, then update the hub summary/docs/fix_plan/galph_memory with MS-SSIM/MAE deltas or blockers.

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
