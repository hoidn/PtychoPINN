Brief:
Guard the working directory (`test "$(pwd -P)" = "/home/ollie/Documents/PtychoPINN"`), export `AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md`, set `HUB="$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier"`, then patch `scripts/compare_models.py` with the new chunked Baseline inference helper (`--baseline-chunk-size`, `--baseline-predict-batch-size`, automatic fallback + per-chunk DIAGNOSTIC logging) so dense-test no longer hits the ResourceExhausted error recorded in `red/blocked_20251116T010000Z_test_baseline_oom.md`.
Rerun `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv | tee "$HUB"/green/pytest_compare_models_translation_fix_v15.log`, then execute the debug-limited compare_models commands followed by the full train/test runs with `--baseline-chunk-size 256 --baseline-predict-batch-size 16` so `analysis/dose_1000/dense/{split}/logs/logs/debug.log` reports non-zero chunk + summary stats and `analysis/dose_1000/dense/{split}/comparison_metrics.csv`/`analysis/metrics_summary.json` contain Baseline rows.
Once Baseline artifacts exist, refresh the Phase D acceptance selectors (`pytest tests/study/test_dose_overlap_overlap.py::{test_filter_dataset_by_mask_handles_scalar_metadata,test_generate_overlap_views_dense_acceptance_floor} -vv` to `$HUB/green/`), run `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber |& tee "$HUB"/cli/run_phase_g_dense_stdout.log`, replay `report_phase_g_dense_metrics.py` + `analyze_dense_metrics.py`, and finish with `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --post-verify-only |& tee "$HUB"/cli/run_phase_g_dense_post_verify_only.log`, filing a `$HUB/red/blocked_<timestamp>.md` if any SSIM grid/verification/preview/highlights/metrics/inventory artifact is still missing.

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
