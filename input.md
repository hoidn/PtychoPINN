Brief:
Set `AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md` and `HUB=$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier`, then rerun both compare_models commands (`--split train` / `--split test`) so the latest Translation failure is captured under `$HUB/cli/phase_g_dense_translation_fix_{split}.log` (blockers → `$HUB/red/blocked_<timestamp>.md`).
Patch `ptycho/custom_layers.ReassemblePatchesLayer` to call `hh.mk_reassemble_position_batched_real` only when `total_patches > batch_size`, documenting the trigger and logging when batching engages. Refactor `_reassemble_position_batched` so it derives `padded_size` from the kwarg/`params`, replaces the per-element `tf.map_fn` resize with a single `tf.image.resize_with_crop_or_pad(batch_translated, padded_size, padded_size)`, and emits explicit shape/dtype diagnostics plus `tf.debugging.assert_equal(tf.shape(canvas), tf.shape(batch_result))` before accumulation (log any cropping counts). Once the helpers are instrumented, keep `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv` GREEN and rerun the compare_models train/test commands to refresh `analysis/dose_1000/dense/{train,test}/comparison_metrics.csv`.
Finish by updating `analysis/blocker.log`, `{analysis}/verification_report.json`, docs/fix_plan.md, plan summary, and galph_memory with the MS-SSIM/MAE deltas plus selector/command references before handing the hub back to the parent study.

Summary: plans/active/FIX-COMPARE-MODELS-TRANSLATION-001/summary.md
Plan: plans/active/FIX-COMPARE-MODELS-TRANSLATION-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
