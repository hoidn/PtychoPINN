Brief:
Stay in `/home/ollie/Documents/PtychoPINN`, export `AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md` and `HUB=$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier`, then rerun `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv | tee "$HUB"/green/pytest_compare_models_translation_fix_v5.log` so the translation guard stays GREEN before touching the hub.
Regenerate both the train and test compare_models bundles (`python -m scripts.compare_models ... --output_dir "$HUB"/analysis/dose_1000/dense/{train,test}`) until Baseline metrics are non-zero and the CSVs use the canonical `Baseline`/`PtyChi` names; if `analysis/dose_1000/dense/test/logs/logs/debug.log` still shows zero amplitudes, capture `$HUB/red/blocked_<timestamp>.md` with the failing command.
Re-run the Phase D acceptance pytest pair, then execute `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber |& tee "$HUB"/cli/run_phase_g_dense_stdout.log` and immediately replay `report_phase_g_dense_metrics.py ...` plus `bin/analyze_dense_metrics.py --hub "$HUB"` so `metrics_summary.*`, `metrics_delta_highlights_preview.txt`, `metrics_digest.md`, and `analysis/artifact_inventory.txt` exist before verifying.
Once metrics succeed, run `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --post-verify-only |& tee "$HUB"/cli/run_phase_g_dense_post_verify_only.log`, and only stop after `{analysis}` contains the SSIM grid summary/log, verification report/log, highlights log, metrics summary/digest, preview text, and artifact inventory with `analysis/verification_report.json` showing 10/10 (otherwise log the blocker under `$HUB/red/` and update summaries).

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
