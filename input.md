Brief:
Guard /home/ollie/Documents/PtychoPINN, export AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md, set HUB="$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier", and keep the translation pytest selector green (tee to $HUB/green/pytest_compare_models_translation_fix_v11.log) before touching the hub.
Instrument/repair scripts/compare_models.py with a fast Baseline-debug loop (log the first patch amplitude/phase, optionally add --baseline-debug-limit or reuse --n-test-groups) and rerun the train/test commands from the plan, capturing $HUB/cli/compare_models_dense_{train,test}_debug.log; stop immediately with $HUB/red/blocked_<timestamp>.md if either split still reports zero-valued `DIAGNOSTIC baseline_output` stats or if canonical Baseline/PtyChi rows are missing from analysis/dose_1000/dense/{split}/comparison_metrics.csv and analysis/metrics_summary.json.
Once Baseline metrics exist, rerun the Phase D acceptance selectors, execute `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber |& tee "$HUB"/cli/run_phase_g_dense_stdout.log`, regenerate the metrics bundle via `report_phase_g_dense_metrics.py` + `analyze_dense_metrics.py` (verify Baseline rows + canonical IDs + preview text), and only then run the fully parameterized `python plans/active/.../bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --post-verify-only |& tee "$HUB"/cli/run_phase_g_dense_post_verify_only.log` so the SSIM grid/verification/highlights/preview/artifact-inventory bundle lands.
Log every blocker under $HUB/red/blocked_<timestamp>.md with the command + error if Baseline stats regress, canonical IDs drop back to aliases, or any artifact required by PREVIEW-PHASE-001 / TEST-CLI-001 remains missing before analysis/verification_report.json reports 10/10.

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
