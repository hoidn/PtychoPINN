Brief:
Guard `/home/ollie/Documents/PtychoPINN`, export `AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md`, set `HUB="$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier"`, and re-run `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv | tee "$HUB"/green/pytest_compare_models_translation_fix_v12.log` before touching the hub.
Update `scripts/compare_models.py` to center flattened `baseline_offsets`, add `--baseline-debug-limit/--baseline-debug-dir` plumbing that logs DIAGNOSTIC stats and dumps NPZ/JSON per split, then rerun the dense-train and dense-test commands with the new flags (tee to `$HUB/cli/compare_models_dense_{split}_debug.log`, stash NPZ under `$HUB/analysis/dose_1000/dense/{split}/debug/`); stop with `$HUB/red/blocked_<timestamp>.md` if either split still reports zero-valued `baseline_output` stats or if `analysis/dose_1000/dense/{split}/comparison_metrics.csv` / `analysis/metrics_summary.json` lack canonical `Baseline` rows.
Once Baseline metrics exist, refresh `pytest tests/study/test_dose_overlap_overlap.py::test_generate_overlap_views_dense_acceptance_floor -vv | tee "$HUB"/green/pytest_dense_acceptance_floor.log`, run `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber |& tee "$HUB"/cli/run_phase_g_dense_stdout.log`, replay `report_phase_g_dense_metrics.py` + `analyze_dense_metrics.py` while verifying Baseline/PtyChi rows + preview text, and immediately invoke `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --post-verify-only |& tee "$HUB"/cli/run_phase_g_dense_post_verify_only.log` so `{analysis}` gains the SSIM/verification/highlights/preview/inventory bundle required by PREVIEW-PHASE-001 / TEST-CLI-001.
Capture every blocker (zero Baseline stats, missing CSV/JSON rows, preview/verifier gaps) under `$HUB/red/blocked_<timestamp>.md` with the failing command before stopping, and only update docs once `analysis/verification_report.json` reports 10/10.

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
