Brief:
Confirm you're in /home/ollie/Documents/PtychoPINN, export AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md, and set HUB="$PWD"/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier so every artifact lands in the canonical report.
Re-run the translation guard (`pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv`) and then execute the debug-limited compare_models commands for train/test with `--baseline-debug-limit 320 --baseline-chunk-size 160 --baseline-predict-batch-size 16`, stopping to file $HUB/red/blocked_*.md if the DIAGNOSTIC stats or CSV rows are still zeroed.
When the debug probes pass, run the full dense train/test compare_models invocations with `--baseline-chunk-size 256 --baseline-predict-batch-size 16` so `analysis/dose_1000/dense/{split}/comparison_metrics.csv` and `analysis/metrics_summary.json` finally carry canonical Baseline rows; no Phase D/G step may proceed until both splits are populated.
Finish the rerun by executing the Phase D acceptance selectors, `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber`, the metrics helpers (`report_phase_g_dense_metrics.py`, `analyze_dense_metrics.py`), and `run_phase_g_dense.py --post-verify-only`, verifying that `{analysis}` now contains SSIM grid, verification, highlights, preview, metrics, and artifact-inventory artifacts (10/10 in verification_report.json) or filing a blocker with the exact command/error if anything is missing.

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
