Brief:
Stay in /home/ollie/Documents/PtychoPINN, `export AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md` and `HUB=$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier`, then keep the translation regression guard green via `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv | tee "$HUB"/green/pytest_compare_models_translation_fix_v6.log`.
Re-run `scripts/compare_models.py` for both train and test (commands in the plan) and do not proceed unless the new `DIAGNOSTIC baseline_input`/`baseline_output` stats in `$HUB/cli/compare_models_dense_{train,test}_rerun.log` show non-zero mean/max/nonzero counts and the corresponding NPZ files contain Baseline signal; if either split is still zeroed, stop immediately and drop `$HUB/red/blocked_<timestamp>.md` referencing the log lines.
Only after both splits emit valid Baseline predictions, rerun the two Phase D acceptance selectors, execute the counted `run_phase_g_dense.py --clobber --dose 1000 --view dense --splits train test`, then replay `report_phase_g_dense_metrics.py` and `bin/analyze_dense_metrics.py` so metrics_summary/digest/preview/artifact_inventory exist.
Finish with `run_phase_g_dense.py --post-verify-only` so SSIM grid + verification/highlights bundle populate `{analysis}`, and verify `analysis/verification_report.json` flips to 10/10 before updating the hub summary/docs/fix_plan/galph_memory (add blockers with exact commands/logs if any step fails).

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
