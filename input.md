Brief:
Stay in /home/ollie/Documents/PtychoPINN, `export AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md`, set `HUB=$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier`, and rerun the translation guard via `pytest … -vv | tee "$HUB"/green/pytest_compare_models_translation_fix_v7.log`.
Regenerate the train/test compare_models bundles (commands in the plan) and halt immediately with `$HUB/red/blocked_<timestamp>.md` if the new `DIAGNOSTIC baseline_input`/`baseline_output` stats or aligned NPZs still show zero Baseline signal; otherwise log the successful stats in `$HUB/cli/compare_models_dense_{train,test}_rerun.log`.
After compare_models passes, rerun the Phase D acceptance selectors, execute `python plans/active/.../bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber`, and verify Phase C/D/E logs plus weight/NPZ artifacts refresh under this repo.
Then rerun `report_phase_g_dense_metrics.py` and `bin/analyze_dense_metrics.py` until `analysis/metrics_summary.*` contains populated `Baseline` and canonical `PtyChi` rows and `analysis/metrics_delta_highlights_preview.txt` exists, followed by `python …/run_phase_g_dense.py --post-verify-only` so `{analysis}` finally holds SSIM grid/verification/highlights/metrics/preview/inventory artifacts; record blockers with exact commands/log excerpts if any step fails before `analysis/verification_report.json` reaches 10/10 and docs/hub/memory are updated.

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
