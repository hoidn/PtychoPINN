Brief:
Guard the repo path by running `test "$(pwd -P)" = "/home/ollie/Documents/PtychoPINN"`, export `AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md` and `HUB=$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier`, then `ls -lh` the hub copies of `data/phase_c/dose_1000/patched_{train,test}.npz`, `data/phase_e/dose_1000/{dense/gs2,baseline/gs1}/wts.h5.zip`, and `data/phase_f/dose_1000/dense/test/ptychi_reconstruction.npz`; if any path is missing, stop and record `$HUB/red/blocked_<timestamp>.md` instead of copying data.
Re-run `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv | tee "$HUB"/green/pytest_compare_models_translation_fix_v18.log` to keep the translation guard GREEN.
Execute the chunked debug compare_models commands (`--baseline-debug-limit 320 --baseline-chunk-size 160 --baseline-predict-batch-size 16`) and then the full chunked commands (`--baseline-chunk-size 256 --baseline-predict-batch-size 16`) for both train and test, teeing to the `_debug_v3.log`/`_full_v3.log` files under `$HUB/cli/` and stopping for a red blocker if DIAGNOSTIC stats or the CSV/JSON Baseline rows stay empty.
After Baseline metrics populate, run the Phase D selectors, `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber`, refresh `report_phase_g_dense_metrics.py` + `analyze_dense_metrics.py`, and finish with the fully parameterized `--post-verify-only` helper so `{analysis}` gains SSIM grid / metrics / preview / verification artifacts (log `$HUB/red/blocked_<timestamp>.md` if PREVIEW-PHASE-001 still fails).

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
