Brief:
Export `AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md` plus `HUB=$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier` and replay the train/test `scripts/compare_models.py` commands (no `--split`) so fresh logs land under `$HUB/cli/phase_g_dense_translation_fix_{train,test}.log`, sending any failures to `$HUB/red/blocked_<timestamp>.md`.
Refactor `ptycho/custom_layers.ReassemblePatchesLayer` so it only calls `hh.mk_reassemble_position_batched_real` when `patch_count > batch_size` and emits tf.print/logs with `patch_count`, `batch_size`, and `padded_size`, then enhance `_reassemble_position_batched` to keep the single `tf.image.resize_with_crop_or_pad` path while logging batch dims/padded_size/crop counts and enforcing the `tf.debugging.assert_equal` shape/dtype guards.
Keep `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv | tee "$HUB"/green/pytest_compare_models_translation_fix.log` GREEN (adding the overlap/intensity conservation check if needed), rerun both compare_models commands until `analysis/dose_1000/dense/{train,test}` refresh, and finish by rewriting `$HUB/analysis/blocker.log` plus `{analysis}/verification_report.json` to show 10/10 validity with MS-SSIM/MAE deltas in the plan/summary.

Summary: plans/active/FIX-COMPARE-MODELS-TRANSLATION-001/summary.md
Plan: plans/active/FIX-COMPARE-MODELS-TRANSLATION-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
