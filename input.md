Brief:
Re-enter `/home/ollie/Documents/PtychoPINN`, export `AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md`, set `HUB="$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier"`, and rerun `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv | tee "$HUB"/green/pytest_compare_models_translation_fix_v14.log` before touching the hub.
Then run `python -m scripts.compare_models` for dense train **and** dense test without the debug limit (tee to `$HUB/cli/compare_models_dense_{split}_full.log`), and stop/file `$HUB/red/blocked_<timestamp>.md` if the new `$HUB/analysis/dose_1000/dense/{split}/logs/logs/debug.log` tail or `comparison_metrics.csv` still shows Baseline mean=0 or blank Baseline rows.
Once Baseline rows exist, refresh `pytest tests/study/test_dose_overlap_overlap.py::{test_filter_dataset_by_mask_handles_scalar_metadata,test_generate_overlap_views_dense_acceptance_floor} -vv` into `$HUB/green/`, execute `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber |& tee "$HUB"/cli/run_phase_g_dense_stdout.log`, immediately rerun `report_phase_g_dense_metrics.py` + `analyze_dense_metrics.py`, and finish with `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --post-verify-only |& tee "$HUB"/cli/run_phase_g_dense_post_verify_only.log` so metrics, highlights, preview, SSIM grid, verification, and inventory artifacts exist and `analysis/verification_report.json` reports 10/10.

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
