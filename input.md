Brief:
Guard the repo/HUB (`test "$(pwd -P)" = "/home/ollie/Documents/PtychoPINN"`, export `AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md`, `HUB=$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier`) and `ls -lh` the Phase C/E/F assets; if any file is missing, stop and capture `$HUB/red/blocked_<timestamp>.md`.
Keep the translation regression tests GREEN via `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv | tee "$HUB"/green/pytest_compare_models_translation_fix_v20.log`.
Run the chunked debug compare_models commands for train/test with `--n-test-groups 320 --pinn-chunk-size 160 --pinn-predict-batch-size 16 --baseline-debug-limit 320 --baseline-chunk-size 160 --baseline-predict-batch-size 16`, teeing to `cli/compare_models_dense_{train,test}_debug_v4.log`, and stop with a red blocker if the DIAGNOSTIC tail or `analysis/dose_1000/dense/{split}/comparison_metrics.csv` still shows zero Baseline stats.
When both debug slices are healthy, rerun the full train/test commands with `--pinn-chunk-size 256 --pinn-predict-batch-size 16 --baseline-chunk-size 256 --baseline-predict-batch-size 16` (logs: `cli/compare_models_dense_{train,test}_full_v4.log`) so the canonical CSV/JSON artifacts gain Baseline rows.
After Baseline metrics exist, execute the Phase D selectors from the plan, then run `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber`, `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/report_phase_g_dense_metrics.py --hub "$HUB"`, `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/analyze_dense_metrics.py --hub "$HUB"`, and the fully parameterized `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --post-verify-only`, logging blockers immediately if any PREVIEW/verification artifact is still missing.

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
