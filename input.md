Brief:
Guard /home/ollie/Documents/PtychoPINN, export AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md, set HUB="$PWD/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier", and rerun `pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv | tee "$HUB"/green/pytest_compare_models_translation_fix_v10.log`.
Instrument/repair `scripts/compare_models.py` (tf.debugging asserts + canonical Baseline/PtyChi IDs) so both train/test runs log non-zero `DIAGNOSTIC baseline_output` stats; rerun the two compare_models commands from the plan, dropping `$HUB/red/blocked_<timestamp>.md` if either split still writes zero-valued Baseline tensors, and capture the refreshed logs under `$HUB`/cli.
After verifying `analysis/metrics_summary.json` and `analysis/dose_1000/dense/test/comparison_metrics.csv` contain Baseline metrics, rerun the Phase D acceptance selectors, execute `python plans/active/.../bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber |& tee "$HUB"/cli/run_phase_g_dense_stdout.log`, and confirm the Phase C/D/E artifacts/logs refresh.
Regenerate the metrics/digest bundle via `report_phase_g_dense_metrics.py` + `analyze_dense_metrics.py` until Baseline rows and `analysis/metrics_delta_highlights_preview.txt` exist, then run the fully parameterized `python plans/active/.../bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --post-verify-only |& tee "$HUB"/cli/run_phase_g_dense_post_verify_only.log`; log blockers with command/error excerpts if the reporter or preview file fails again before `{analysis}/verification_report.json` reaches 10/10.

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
