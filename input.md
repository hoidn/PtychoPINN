Brief:
Guard the repo root (`test "$(pwd -P)" = "/home/ollie/Documents/PtychoPINN"`) and export `AUTHORITATIVE_CMDS_DOC=./docs/TESTING_GUIDE.md` plus `HUB="$PWD"/plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/reports/2025-11-12T010500Z/phase_g_dense_full_run_verifier` so every log/artifact tees into the canonical report per TEST-CLI-001 / TYPE-PATH-001.
Re-run the translation regression selector (`pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv | tee "$HUB"/green/pytest_compare_models_translation_fix_v16.log`) before touching compare_models.
Exercise the chunked Baseline helper on the debug-limited slice (`--baseline-debug-limit 320 --baseline-chunk-size 160 --baseline-predict-batch-size 16` for both train/test) and stop immediately with `$HUB/red/blocked_<timestamp>.md` if the DIAGNOSTIC stats or `analysis/dose_1000/dense/{split}/comparison_metrics.csv` still show zero Baseline rows.
When the debug probes are healthy, rerun the full dense train/test compare_models commands with `--baseline-chunk-size 256 --baseline-predict-batch-size 16`, verify both CSVs plus `analysis/metrics_summary.json` now contain Baseline entries, and only then proceed.
Finish the rerun by executing the Phase D acceptance selectors, `python plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/bin/run_phase_g_dense.py --hub "$HUB" --dose 1000 --view dense --splits train test --clobber`, `report_phase_g_dense_metrics.py`, `analyze_dense_metrics.py`, and the fully parameterized `run_phase_g_dense.py --post-verify-only`, confirming `{analysis}` now has the SSIM grid, verification (10/10), highlights, preview, metrics, and artifact-inventory bundle or logging a blocker with the exact failing command/signature.

Summary: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/summary.md
Plan: plans/active/STUDY-SYNTH-FLY64-DOSE-OVERLAP-001/implementation.md
Selector: pytest tests/study/test_dose_overlap_comparison.py::{test_pinn_reconstruction_reassembles_batched_predictions,test_pinn_reconstruction_reassembles_full_train_split} -vv
